name: Django CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: ["3.9"]

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      run: |
        python manage.py test

    - name: deploy_setup
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIAXDJ22J2QWU5KEYF6
        aws-secret-access-key: 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
        aws-region: ap-southeast-1
    
    - name: s3_upload
      run: aws deploy push --application-name django_django --description "DjangoTestApp" --s3-location s3://new-buckit-152/server/build.zip --source .
      
    - name: code_deploy
      run: aws deploy create-deployment --application-name django_django --deployment-config-name CodeDeployDefault.OneAtATime --deployment-group-name django_django_deploy --s3-location bucket=new-buckit-152,bundleType=zip,key=server/build.zip
    
    
